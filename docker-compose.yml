version: '3.8'

services:
  movie-crs-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: movie-crs-api
    ports:
      - "8000:8000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - LLM_MODEL=${LLM_MODEL:-gpt-4o-mini}
      - TEMPERATURE=${TEMPERATURE:-0.7}
      - MAX_TOKENS=${MAX_TOKENS:-1000}
      - EMBEDDING_MODEL=${EMBEDDING_MODEL:-sentence-transformers/all-MiniLM-L6-v2}
      - VECTOR_STORE_TYPE=${VECTOR_STORE_TYPE:-chroma}
      - VECTOR_STORE_PATH=${VECTOR_STORE_PATH:-./data/vector_store}
      - API_HOST=${API_HOST:-0.0.0.0}
      - API_PORT=${API_PORT:-8000}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - MAX_CONCURRENT_REQUESTS=${MAX_CONCURRENT_REQUESTS:-10}
      - REQUEST_TIMEOUT=${REQUEST_TIMEOUT:-30}
    volumes:
      # Persist vector store data
      - vector-store-data:/app/data/vector_store
      # Persist logs
      - logs-data:/app/logs
      # Stream application log back to the host
      - ./app.log:/app/app.log
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - movie-crs-network

  # Optional: Add nginx for load balancing in production
  # nginx:
  #   image: nginx:alpine
  #   container_name: movie-crs-nginx
  #   ports:
  #     - "80:80"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #   depends_on:
  #     - movie-crs-api
  #   networks:
  #     - movie-crs-network

volumes:
  vector-store-data:
    driver: local
  logs-data:
    driver: local

networks:
  movie-crs-network:
    driver: bridge
